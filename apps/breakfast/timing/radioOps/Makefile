COMPONENT=TestC

TEST_INTERVAL?=100
PACKET_SIZE?=30
SEND_PAUSE?=0
STOP_PAUSE?=0

CFLAGS+= -DTEST_INTERVAL=$(TEST_INTERVAL)
CFLAGS+= -DPACKET_SIZE=$(PACKET_SIZE)
CFLAGS+= -DSEND_PAUSE=$(SEND_PAUSE)
CFLAGS+= -DSTOP_PAUSE=$(STOP_PAUSE)

PFLAGS+=-I$(TOSDIR)/lib/printf
include $(MAKERULES)
