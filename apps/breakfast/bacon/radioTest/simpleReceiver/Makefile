COMPONENT=RadioTestC

IS_SENDER?=0
SEND_INTERVAL?=100
BATCH_NUM?=0
POWER_LEVEL?=0

CFLAGS+=-DIS_SENDER=$(IS_SENDER)
CFLAGS+=-DSEND_INTERVAL=$(SEND_INTERVAL)
CFLAGS+=-DBATCH_NUM=$(BATCH_NUM)
CFLAGS+=-DPOWER_LEVEL=$(POWER_LEVEL)

CFLAGS+=-I..
PFLAGS += -I$(TOSDIR)/lib/printf
PFLAGS+=-I$(OSIAN_ROOT)/tinyos/tos/platforms/bacon/hardware/leds
include $(MAKERULES)
